import React from 'react';
import { Box } from '@mui/material'; // Import necessary components from Material-UI

function HeroSection() {
  return (
    <Box
      sx={{
        backgroundImage: `url('./banner.jpg')`, // Set the background image
        // backgroundSize: 'cover', // Cover the entire area of the box
        // backgroundPosition: 'center', // Center the background image
        // minHeight: '100vh', // Set the minimum height to the viewport height
        // display: 'flex',
        // alignItems: 'center',
        // justifyContent: 'center',
        // textAlign: 'center',
        // color: 'white', // Text color
        // padding: '20px', // Adjust padding as needed
      }}
    >
    </Box>
  );
}

export default HeroSection;
